// Proof-of-resolution export for the workspace foundation task; remove once
// IMPLEMENT_CORE_GAME_RULES replaces it with real domain types and rules.
export const CORE_PACKAGE_NAME = '@mine-sweeper/core' as const;
